const state = {
  publishDateDisplayFormat: window[import.meta.env.VITE_APP_NAME].STORE.config.publishDateDisplayFormat
}

const getters = {}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
